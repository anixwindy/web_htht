# 2026-07-27 — 工作回報 #0：方向定案 & 工作區建立

> 交接用：下次開機**從這裡繼續**。

## 這個休假窗做了什麼
- **定案主線**：C# + MonoGame，純 code-first 做遊戲，磨一輩子。
- 想清楚為什麼是這條：純 IDE + F5、不碰 Unity/Godot 引擎編輯器、C# 的 GC = 少記憶體、選離散/回合制類型 = 少數學少複雜 DS、美術可跳過（色塊/數字）。
- 工具定案：**Visual Studio Community（免費）**——因為做 C# 它的 F5「一按就通」，摩擦最低。
- 建好工作區 `D:\MonoGame`（含 `CLAUDE.md`）＋ 這個工作回報站 `web/`（由舊 pwn 站複製改寫）。

## 卡在哪 / 待解
- 還沒真正寫任何一行 MonoGame 的 code——目前都是規劃。
- 環境還沒裝：Visual Studio Community + MonoGame 範本。

## 下次開機第一步
1. 裝 **Visual Studio Community**，勾 .NET 桌面開發。
2. 裝 **MonoGame 範本**，開一個 `Blocks/` 新專案，按 **F5** → 出現一片藍色視窗（那就是遊戲畫面）。
3. 讓那片畫面上出現一個**能被方向鍵推動的方塊**（先發後深的第一個「地板」）。

## 備註
- 專案：（尚無）　｜　有沒有 export / 上線：否
